// TODO: README.md에 정확한 API 경로가 아직 명시 안 됨.
// 참고: "6. 마이 페이지" — 점수, 마일리지, 마일리지 결제 히스토리, 풀이 시간

export function getMyProfile() {
  throw new Error("getMyProfile: API 경로 미정");
}

export function getMyMileageHistory() {
  throw new Error("getMyMileageHistory: API 경로 미정");
}
